langkah-langkah
1. install appium terlebih dahulu secara global menggunakan command "npm install -g appium"
2. lalu install uianimator2 dengan command "appium driver install uiautomator2"
3. dan jalankan di cmd/terminal appiumnya menggunakan command "appium"
4. lalu jalankan command "npm install"
5. jalankan perintah "node app.js" - untuk menjalankan api test
6. jalankan perintah "node mobile.js" - untuk menjalankan mobile test
